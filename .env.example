# Example environment variables for VoiceGuard
# Copy to .env and fill in real values (never commit .env)

# Web app config
# Default is http://localhost:8080 if not set
VITE_API_URL=http://localhost:8080

# OPENAI_API_KEY=
# DATABASE_URL=
